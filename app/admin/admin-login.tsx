"use client";
import { useState } from "react";
import Link from "next/link";
export default function AdminLogin(){const [error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const data=new FormData(e.currentTarget);const r=await fetch("/api/admin/session",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({username:data.get("username"),password:data.get("password")})});const body=await r.json();if(r.ok)location.reload();else{setError(body.error||"Unable to sign in.");setBusy(false)}}return <main className="admin-login"><Link className="login-brand" href="/" aria-label="Printly home"><img src="/assets/printly-logo.png" alt=""/></Link><form onSubmit={submit}><span>PRINTLY / PRIVATE ACCESS</span><h1>Manage the work.</h1><p>Sign in to add, edit, arrange, feature, hide, or remove portfolio projects.</p><label>Username<input name="username" autoComplete="username" required autoFocus/></label><label>Password<input name="password" type="password" autoComplete="current-password" required/></label>{error&&<div className="login-error" role="alert">{error}</div>}<button disabled={busy}>{busy?"Signing in…":"Enter dashboard →"}</button><Link href="/">← Back to Printly</Link></form></main>}
