import {NextResponse} from "next/server";import {isAdminRequest} from "../../../lib/admin-auth";import {db} from "../../../lib/projects";import {deleteFile,uploadFile} from "../../../lib/storage";
export async function POST(request:Request){if(!isAdminRequest(request))return NextResponse.json({error:"Not authorized"},{status:403});const form=await request.formData(),id=Number(form.get("project_id")),file=form.get("image");if(!id||!(file instanceof File)||!file.type.startsWith("image/")||file.size>12*1024*1024)return NextResponse.json({error:"Use an image under 12 MB."},{status:400});const ext=(file.name.split(".").pop()||"jpg").replace(/[^a-z0-9]/gi,"").toLowerCase(),key=`projects/covers/${id}-${Date.now()}.${ext}`,sql=db(),old=(await sql.query(`SELECT image_key FROM projects WHERE id=$1`,[id]))[0] as {image_key:string|null}|undefined,imageUrl=await uploadFile(key,file);await sql.query(`UPDATE projects SET image_url=$1,image_key=$2,updated_at=NOW() WHERE id=$3`,[imageUrl,key,id]);await deleteFile(old?.image_key);return NextResponse.json({ok:true});}
